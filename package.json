{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [
    "image-upload",
    "express",
    "node"
  ],
  "author": "",
  "license": "ISC",
  "description": "Backend server for image upload application",
  "dependencies": {
    "axios": "^1.9.0",
    "backend": "file:",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
